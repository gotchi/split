#include <stdio.h>
#include "define.h"
#include "split.h"

/**
 * @brief split処理
 * 
 * @param string 対象文字列
 * @param resolve 区切り文字列
 * @return char** 区切った単語の2次元配列
 */
WORDSLIST* split(char *string, char *resolve)
{

    return NULL;    /* error */
}